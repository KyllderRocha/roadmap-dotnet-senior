name: .NET CI/CD

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Restore dependencies
        run: dotnet restore
        working-directory: ./fase-2-task-manager-clean

      - name: Build
        run: dotnet build --no-restore
        working-directory: ./fase-2-task-manager-clean

      - name: Convert repository name to lowercase
        id: strings
        shell: bash
        run: echo "repo_lowercase=$(echo ${{ github.repository }} | tr '[:upper:]' '[:lower:]')" >> $GITHUB_OUTPUT

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./fase-2-task-manager-clean
          file: ./fase-2-task-manager-clean/Dockerfile
          push: true
          tags: ghcr.io/${{ steps.strings.outputs.repo_lowercase }}:${{ github.sha }}